name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  packages: write   # nécessaire pour pousser vers ghcr.io

jobs:
  # ------------------------------------------------------------------ #
  # Étape 1 — Qualité du code                                           #
  # ------------------------------------------------------------------ #
  lint-and-test:
    name: Lint & Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: src/app

    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '25'
          cache: 'npm'
          cache-dependency-path: src/app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

  # ------------------------------------------------------------------ #
  # Étape 2 — Build de l'image Docker                                   #
  # Dépend de lint-and-test (fail fast)                                 #
  # ------------------------------------------------------------------ #

  build:
    name: Build & Publish image
    runs-on: ubuntu-latest
    needs: lint-and-test

    steps:
      - uses: actions/checkout@v5

      - name: Set lower case image name
        run: |
          # On prend "Kevendeuh/formation-devops" et on force tout en minuscules
          REPO_LOWER=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          # On crée la variable finale et on l'injecte dans l'environnement GitHub
          echo "IMAGE_NAME=ghcr.io/$REPO_LOWER" >> $GITHUB_ENV

      # 2. Authentification
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 3. Build et Push avec la variable propre
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: src/app
          push: ${{ github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' }}
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
